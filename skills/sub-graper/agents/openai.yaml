interface:
  display_name: "Sub-Graper"
  short_description: "Delegate and cache codebase searches"
  default_prompt: "Use $sub-graper to locate and explain code while keeping search noise out of the main context."

policy:
  allow_implicit_invocation: false
