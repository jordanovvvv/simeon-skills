interface:
  display_name: "Sub-Graper"
  short_description: "Run bounded delegated code searches"
  default_prompt: "Use $sub-graper after $sub-router selects an isolated code search."

policy:
  allow_implicit_invocation: false
